/**
 * Static properties and methods for the query control view.
 *
 * This object can be extended with static methods when passed to
 * Elementor's control registration. It serves as a placeholder for any
 * future static methods that might be needed for the controls.
 *
 * @type {Object}
 */
export const BaseQueryControlViewStatic = {
  // This empty object will be extended when registering controls
  // Each control can add its own static methods if needed
}
