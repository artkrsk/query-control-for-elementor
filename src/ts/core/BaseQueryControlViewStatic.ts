/**
 * Static properties for BaseQueryControlView
 *
 * This object contains static properties that are merged into the
 * control class rather than instances. Currently empty but can be
 * extended for future static method needs.
 */
export const BaseQueryControlViewStatic = {}
