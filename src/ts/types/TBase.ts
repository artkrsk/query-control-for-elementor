import type { Editor } from '@artemsemkin/elementor-types'

/**
 * Base type alias for Elementor Select2 control view
 * Used as the parent type for query control context
 */
export type TBase = Editor.Controls.ControlBaseDataView
